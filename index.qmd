---
format: 
  revealjs:
    #theme: ["theme/q-theme.scss"]
    slide-number: c/t
    logo: "https://www.insp.mx/assets/images/insp.png"
    footer: "[github.com/fdzul](https://github.com/fdzul)"
    code-copy: true
    center-title-slide: false
highlight-style: a11y
code-link: true
height: 1080
width: 1600
execute: 
  eval: true
  echo: true
---

<h1>Análisis Espacial del Dengue<br>en México</h1>

<h2>

</h2>

<hr>

<br>


<h3>Dr. Felipe Dzul Manzanilla</h3>

<br>


<h3>Dr. Fabián Correa-Morales</h3>

<br> <br>

<h3>Create: 2024-08-13</h3>

<br>

![](heatmap_tepic.png){.absolute top="250" left="750" width="800"}



## [Transmisión Activa del Dengue en Nayarit]{ style="color:#003300;"}
<hr style="height:2px;border-width:0;color:#330019;background-color:#330019">

<html>
<head>
```{=html}
<style>
.grid-container {
  display: grid;
  grid-template-columns: auto auto auto auto;
  padding: 5px;
}

.grid-item {
  background-color: rgba(255, 255, 255, 0.8);
  border: 1px solid rgba(0, 0, 0, 0.8);
  padding: 5px;
  font-size: 30px;
  text-align: center;
}
</style>
```
</head>
<body>
<h1>
</h1>
<p>
</p>

::: grid-container

::: grid-item
Semana
:::

::: grid-item
Tepic
:::

::: grid-item
Bahia de Banderas
:::

::: grid-item
San Blas
:::

::: grid-item
Acumulado
:::

::: grid-item
```{r tepic_acumulado}
#| echo: false
#| output: true
# Step 1. load the dengue dataset geocoded ####
load("/Users/fdzul/Library/CloudStorage/OneDrive-Personal/proyects/geocoding_mex/2024/8.RData/denmex_2024.RData")

########
densnv::mp_heatmap(locality = c("Tepic", "Xalisco"),
                        cve_edo = "18",
                        geocoded_datasets = z,
                        zoom = 13,
                        status_caso = c(1, 2),
                        week = seq(from = 1, to = lubridate::epiweek(Sys.Date()), by = 1),
                        alpha = 0.2,
                        static = TRUE,
                        map_type = "satellite")
```

:::

::: grid-item
```{r bahia_acumulado}
#| echo: false
#| output: true
# Step 1. load the dengue dataset geocoded ####
########
densnv::mp_heatmap(locality = "Bahia de Banderas",
                        cve_edo = "18",
                        geocoded_datasets = z,
                        zoom = 16,
                        status_caso = c(1, 2),
                        week = seq(from = 1, to = lubridate::epiweek(Sys.Date()), by = 1),
                        alpha = 0.2,
                        static = TRUE,
                        map_type = "satellite")
```

:::

::: grid-item

```{r San_blas_acumulado}
#| echo: false
#| output: true

########
densnv::mp_heatmap(locality = "San Blas",
                        cve_edo = "18",
                        geocoded_datasets = z,
                        zoom = 15,
                        status_caso = c(1, 2),
                        week = seq(from = 1, to = lubridate::epiweek(Sys.Date()), by = 1),
                        alpha = 0.2,
                        static = TRUE,
                        map_type = "satellite")
```
:::

::: grid-item
Último Més
:::

::: grid-item
```{r tepic_ultimo_mes}
#| echo: false
#| output: true
# Step 1. load the dengue dataset geocoded ####

########
densnv::mp_heatmap(locality = c("Tepic", "Xalisco"),
                        cve_edo = "18",
                        geocoded_datasets = z,
                        zoom = 13,
                        status_caso = c(1, 2),
                        week = seq(from = lubridate::epiweek(Sys.Date())-4, to = lubridate::epiweek(Sys.Date()), by = 1),
                        alpha = 0.2,
                        static = TRUE,
                        map_type = "satellite")
```

:::

::: grid-item
```{r bahia_ultimo_mes, error=FALSE}
#| echo: false
#| output: true
# Step 1. load the dengue dataset geocoded ####
########
densnv::mp_heatmap(locality = "Bahia de Banderas",
                        cve_edo = "18",
                        geocoded_datasets = z,
                        zoom = 16,
                        status_caso = c( 2),
                        week = seq(from = lubridate::epiweek(Sys.Date())-4, 
                                   to = lubridate::epiweek(Sys.Date()), 
                                   by = 1),
                        alpha = 0.2,
                        static = TRUE,
                        map_type = "satellite")
```

:::

::: grid-item

```{r San_blas_ultimo_mes}
#| echo: false
#| output: true

########
densnv::mp_heatmap(locality = "San Blas",
                        cve_edo = "18",
                        geocoded_datasets = z,
                        zoom = 15,
                        status_caso = c(1, 2),
                        week = seq(from = lubridate::epiweek(Sys.Date())-4, to = lubridate::epiweek(Sys.Date()), by = 1),
                        alpha = 0.2,
                        static = TRUE,
                        map_type = "satellite")
```
:::

::: grid-item
Últimas Dos Semanas
:::

::: grid-item
```{r tepic_ultimas_dos_semanas}
#| echo: false
#| output: true
# Step 1. load the dengue dataset geocoded ####

########
densnv::mp_heatmap(locality = c("Tepic", "Xalisco"),
                        cve_edo = "18",
                        geocoded_datasets = z,
                        zoom = 13,
                        status_caso = c(1, 2),
                        week = seq(from = lubridate::epiweek(Sys.Date())-2, to = lubridate::epiweek(Sys.Date()), by = 1),
                        alpha = 0.2,
                        static = TRUE,
                        map_type = "satellite")
```

:::

::: grid-item
```{r bahia_ultimas_dos_semanas}
#| echo: false
#| output: true
# Step 1. load the dengue dataset geocoded ####
########
densnv::mp_heatmap(locality = "Bahia de Banderas",
                        cve_edo = "18",
                        geocoded_datasets = z,
                        zoom = 16,
                        status_caso = c( 2),
                        week = seq(from = lubridate::epiweek(Sys.Date())-2, to = lubridate::epiweek(Sys.Date()), by = 1),
                        alpha = 0.2,
                        static = TRUE,
                        map_type = "satellite")
```

:::

::: grid-item

```{r San_blas_ultimas_dos_semanas}
#| echo: false
#| output: true

########
densnv::mp_heatmap(locality = "San Blas",
                        cve_edo = "18",
                        geocoded_datasets = z,
                        zoom = 15,
                        status_caso = c(1, 2),
                        week = seq(from = lubridate::epiweek(Sys.Date())-2, to = lubridate::epiweek(Sys.Date()), by = 1),
                        alpha = 0.2,
                        static = TRUE,
                        map_type = "satellite")
```
:::


:::

</body>
</html>

## [Transmisión Activa del Dengue en Nayarit]{ style="color:#003300;"}
<hr style="height:2px;border-width:0;color:#330019;background-color:#330019">

<html>
<head>
```{=html}
<style>
.grid-container {
  display: grid;
  grid-template-columns: auto auto auto auto;
  padding: 5px;
}

.grid-item {
  background-color: rgba(255, 255, 255, 0.8);
  border: 1px solid rgba(0, 0, 0, 0.8);
  padding: 5px;
  font-size: 30px;
  text-align: center;
}
</style>
```
</head>
<body>
<h1>
</h1>
<p>
</p>

::: grid-container

::: grid-item
Semana
:::

::: grid-item
Santiago Ixcuintla
:::

::: grid-item
Tecuala
:::

::: grid-item
Tuxpan
:::

::: grid-item
Acumulado
:::

::: grid-item
```{r santiago_acumulado}
#| echo: false
#| output: true
densnv::mp_heatmap(locality = "Santiago Ixcuintla",
                        cve_edo = "18",
                        geocoded_datasets = z,
                        zoom = 15,
                        status_caso = c(1, 2),
                        week = seq(from = 1, to = lubridate::epiweek(Sys.Date()), by = 1),
                        alpha = 0.2,
                        static = TRUE,
                        map_type = "satellite")
```

:::

::: grid-item
```{r tecuala_acumulado}
#| echo: false
#| output: true
# Step 1. load the dengue dataset geocoded ####
densnv::mp_heatmap(locality = "Tecuala",
                   cve_edo = "18",
                   geocoded_datasets = z,
                   zoom = 15,
                   status_caso = c(1, 2),
                   week = seq(from = 1, to = lubridate::epiweek(Sys.Date()), by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")

```

:::

::: grid-item

```{r tuxpan_acumulado}
#| echo: false
#| output: true
densnv::mp_heatmap(locality = "Tuxpan",
                   cve_edo = "18",
                   geocoded_datasets = z,
                   zoom = 15,
                   status_caso = c(1, 2),
                   week = seq(from = 1, to = lubridate::epiweek(Sys.Date()), by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")
```
:::

::: grid-item
Último Mes
:::

::: grid-item
```{r santiago_ultimo_mes}
#| echo: false
#| output: true
densnv::mp_heatmap(locality = "Santiago Ixcuintla",
                        cve_edo = "18",
                        geocoded_datasets = z,
                        zoom = 15,
                        status_caso = c(1, 2),
                        week = seq(from = lubridate::epiweek(Sys.Date())-4, to = lubridate::epiweek(Sys.Date()), by = 1),
                        alpha = 0.2,
                        static = TRUE,
                        map_type = "satellite")
```

:::

::: grid-item
```{r tecuala_ultimo_mes}
#| echo: false
#| output: true
# Step 1. load the dengue dataset geocoded ####
########
densnv::mp_heatmap(locality = "Tecuala",
                   cve_edo = "18",
                   geocoded_datasets = z,
                   zoom = 15,
                   status_caso = c(1, 2),
                   week = seq(from = lubridate::epiweek(Sys.Date())-4, to = lubridate::epiweek(Sys.Date()), by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")
```

:::

::: grid-item

```{r tuxpan_ultimo_mes}
#| echo: false
#| output: true
densnv::mp_heatmap(locality = "Tuxpan",
                   cve_edo = "18",
                   geocoded_datasets = z,
                   zoom = 15,
                   status_caso = c(1, 2),
                   week = seq(from = lubridate::epiweek(Sys.Date())-4, to = lubridate::epiweek(Sys.Date()), by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")
```
:::

::: grid-item
Últimas dos Semanas
:::

::: grid-item
```{r santiago_ultimas_dos_semanas}
#| echo: false
#| output: true
densnv::mp_heatmap(locality = "Santiago Ixcuintla",
                        cve_edo = "18",
                        geocoded_datasets = z,
                        zoom = 15,
                        status_caso = c(1, 2),
                        week = seq(from = lubridate::epiweek(Sys.Date())-2, to = lubridate::epiweek(Sys.Date()), by = 1),
                        alpha = 0.2,
                        static = TRUE,
                        map_type = "satellite")

```

:::

::: grid-item
```{r tecuala_ultimas_dos_semanas}
#| echo: false
#| output: true
# Step 1. load the dengue dataset geocoded ####
densnv::mp_heatmap(locality = "Tecuala",
                   cve_edo = "18",
                   geocoded_datasets = z,
                   zoom = 15,
                   status_caso = c(1, 2),
                   week = seq(from = lubridate::epiweek(Sys.Date())-2, to = lubridate::epiweek(Sys.Date()), by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")
```

:::

::: grid-item

```{r tuxpan_ultimas_dos_semanas}
#| echo: false
#| output: true
densnv::mp_heatmap(locality = "Tuxpan",
                   cve_edo = "18",
                   geocoded_datasets = z,
                   zoom = 15,
                   status_caso = c(1, 2),
                   week = seq(from = lubridate::epiweek(Sys.Date())-2, to = lubridate::epiweek(Sys.Date()), by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")
```
:::


:::

</body>
</html>



## [Transmisión Activa del Dengue en Morelos]{ style="color:#003300;"}
<hr style="height:2px;border-width:0;color:#330019;background-color:#330019">

<html>
<head>
```{=html}
<style>
.grid-container {
  display: grid;
  grid-template-columns: auto auto auto auto;
  padding: 5px;
}

.grid-item {
  background-color: rgba(255, 255, 255, 0.8);
  border: 1px solid rgba(0, 0, 0, 0.8);
  padding: 5px;
  font-size: 30px;
  text-align: center;
}
</style>
```
</head>
<body>
<h1>
</h1>
<p>
</p>

::: grid-container

::: grid-item
Semana
:::

::: grid-item
Cuernavaca
:::

::: grid-item
Juitepec
:::

::: grid-item
Temixco
:::

::: grid-item
Acumulado
:::

::: grid-item
```{r cuernavaca_acumulado}
#| echo: false
#| output: true
densnv::mp_heatmap(locality = "Cuernavaca",
                   cve_edo = "17",
                   geocoded_datasets = z,
                   zoom = 13,
                   status_caso = c(1, 2),
                   week = seq(from = 1, to = lubridate::epiweek(Sys.Date()), by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")
```

:::

::: grid-item
```{r juitepec_acumulado}
#| echo: false
#| output: true
# Step 1. load the dengue dataset geocoded ####
densnv::mp_heatmap(locality = "Jiutepec",
                   cve_edo = "17",
                   geocoded_datasets = z,
                   zoom = 13,
                   status_caso = c(1, 2),
                   week = seq(from = 1, to = lubridate::epiweek(Sys.Date()), by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")

```

:::

::: grid-item

```{r temixco_acumulado}
#| echo: false
#| output: true
densnv::mp_heatmap(locality = "Temixco",
                    cve_edo = "17",
                    geocoded_datasets = z,
                    zoom = 13,
                    status_caso = c(1, 2),
                    week = seq(from = 1, to = lubridate::epiweek(Sys.Date()), by = 1),
                    alpha = 0.2,
                    static = TRUE,
                    map_type = "satellite")
```
:::

::: grid-item
Último Mes
:::

::: grid-item
```{r cuernavaca_ultimo_mes}
#| echo: false
#| output: true
densnv::mp_heatmap(locality = "Cuernavaca",
                   cve_edo = "17",
                   geocoded_datasets = z,
                   zoom = 13,
                   status_caso = c(1, 2),
                   week = seq(from = lubridate::epiweek(Sys.Date())-4, to = lubridate::epiweek(Sys.Date()), by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")
```

:::

::: grid-item
```{r jiutepec_ultimo_mes}
#| echo: false
#| output: true
# Step 1. load the dengue dataset geocoded ####
########
densnv::mp_heatmap(locality = "Jiutepec",
                   cve_edo = "17",
                   geocoded_datasets = z,
                   zoom = 13,
                   status_caso = c(1, 2),
                   week = seq(from = lubridate::epiweek(Sys.Date())-4, to = lubridate::epiweek(Sys.Date()), by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")
```

:::

::: grid-item

```{r temixco_ultimo_mes}
#| echo: false
#| output: true
densnv::mp_heatmap(locality = "Temixco",
                    cve_edo = "17",
                    geocoded_datasets = z,
                    zoom = 13,
                    status_caso = c(1, 2),
                    week = seq(from = lubridate::epiweek(Sys.Date())-4, to = lubridate::epiweek(Sys.Date()), by = 1),
                    alpha = 0.2,
                    static = TRUE,
                    map_type = "satellite")
```
:::

::: grid-item
Últimas dos Semanas
:::

::: grid-item
```{r cuernavaca_ultimas_dos_semanas}
#| echo: false
#| output: true
densnv::mp_heatmap(locality = "Cuernavaca",
                   cve_edo = "17",
                   geocoded_datasets = z,
                   zoom = 13,
                   status_caso = c(1, 2),
                   week = seq(from = lubridate::epiweek(Sys.Date())-2, to = lubridate::epiweek(Sys.Date()), by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")

```

:::

::: grid-item
```{r juitepec_ultimas_dos_semanas}
#| echo: false
#| output: true
# Step 1. load the dengue dataset geocoded ####
densnv::mp_heatmap(locality = "Jiutepec",
                   cve_edo = "17",
                   geocoded_datasets = z,
                   zoom = 13,
                   status_caso = c(1, 2),
                   week = seq(from = lubridate::epiweek(Sys.Date())-2, to = lubridate::epiweek(Sys.Date()), by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")
```

:::

::: grid-item

```{r temixco_ultimas_dos_semanas}
#| echo: false
#| output: true
densnv::mp_heatmap(locality = "Temixco",
                    cve_edo = "17",
                    geocoded_datasets = z,
                    zoom = 13,
                    status_caso = c(1, 2),
                    week = seq(from = lubridate::epiweek(Sys.Date())-2, to = lubridate::epiweek(Sys.Date()), by = 1),
                    alpha = 0.2,
                    static = TRUE,
                    map_type = "satellite")
```
:::


:::

</body>
</html>


## [Transmisión Activa del Dengue en Morelos]{ style="color:#003300;"}
<hr style="height:2px;border-width:0;color:#330019;background-color:#330019">

<html>
<head>
```{=html}
<style>
.grid-container {
  display: grid;
  grid-template-columns: auto auto auto auto;
  padding: 5px;
}

.grid-item {
  background-color: rgba(255, 255, 255, 0.8);
  border: 1px solid rgba(0, 0, 0, 0.8);
  padding: 5px;
  font-size: 30px;
  text-align: center;
}
</style>
```
</head>
<body>
<h1>
</h1>
<p>
</p>

::: grid-container

::: grid-item
Semana
:::

::: grid-item
Cuautla
:::

::: grid-item
Jujutla
:::

::: grid-item
Emiliano Zapata
:::

::: grid-item
Acumulado
:::

::: grid-item
```{r cuautla_acumulado}
#| echo: false
#| output: true
densnv::mp_heatmap(locality = "Cuautla",
                   cve_edo = "17",
                   geocoded_datasets = z,
                   zoom = 13,
                   status_caso = c(1, 2),
                   week = seq(from = 1, to = lubridate::epiweek(Sys.Date()), by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")
```

:::

::: grid-item
```{r jojutla_acumulado}
#| echo: false
#| output: true
# Step 1. load the dengue dataset geocoded ####
densnv::mp_heatmap(locality = "Jojutla",
                   cve_edo = "17",
                   geocoded_datasets = z,
                   zoom = 16,
                   status_caso = c(1, 2),
                   week = seq(from = 1, to = lubridate::epiweek(Sys.Date()), by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")

```

:::

::: grid-item

```{r emiliano_zapata_acumulado}
#| echo: false
#| output: true
densnv::mp_heatmap(locality = "Emiliano Zapata",
                   cve_edo = "17",
                   geocoded_datasets = z,
                   zoom = 13,
                   status_caso = c(1, 2),
                   week = seq(from = 1, to = lubridate::epiweek(Sys.Date()), by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")
```
:::

::: grid-item
Último Mes
:::

::: grid-item
```{r cuautla_ultimo_mes}
#| echo: false
#| output: true
densnv::mp_heatmap(locality = "Cuautla",
                   cve_edo = "17",
                   geocoded_datasets = z,
                   zoom = 13,
                   status_caso = c(1, 2),
                   week = seq(from = lubridate::epiweek(Sys.Date())-4, to = lubridate::epiweek(Sys.Date()), by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")
```

:::

::: grid-item
```{r jojutla_ultimo_mes}
#| echo: false
#| output: true
# Step 1. load the dengue dataset geocoded ####
densnv::mp_heatmap(locality = "Jojutla",
                   cve_edo = "17",
                   geocoded_datasets = z,
                   zoom = 16,
                   status_caso = c(1, 2),
                   week = seq(from = lubridate::epiweek(Sys.Date())-4, to = lubridate::epiweek(Sys.Date()), by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")

```

:::

::: grid-item

```{r emiliano_zapata_ultimo_mes}
#| echo: false
#| output: true
densnv::mp_heatmap(locality = "Emiliano Zapata",
                   cve_edo = "17",
                   geocoded_datasets = z,
                   zoom = 13,
                   status_caso = c(1, 2),
                   week = seq(from = lubridate::epiweek(Sys.Date())-4, to = lubridate::epiweek(Sys.Date()), by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")
```
:::

::: grid-item
Últimas dos Semanas
:::

::: grid-item
```{r cuautla_ultimas_dos_semanas}
#| echo: false
#| output: true
densnv::mp_heatmap(locality = "Cuautla",
                   cve_edo = "17",
                   geocoded_datasets = z,
                   zoom = 13,
                   status_caso = c(1, 2),
                   week = seq(from = lubridate::epiweek(Sys.Date())-2, to = lubridate::epiweek(Sys.Date()), by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")

```

:::

::: grid-item
```{r jojutla_ultimas_dos_semanas}
#| echo: false
#| output: true
# Step 1. load the dengue dataset geocoded ####
densnv::mp_heatmap(locality = "Jojutla",
                   cve_edo = "17",
                   geocoded_datasets = z,
                   zoom = 16,
                   status_caso = c(1, 2),
                   week = seq(from = lubridate::epiweek(Sys.Date())-2, to = lubridate::epiweek(Sys.Date()), by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")
```

:::

::: grid-item

```{r emiliano_zapata_ultimas_dos_semanas}
#| echo: false
#| output: true
densnv::mp_heatmap(locality = "Emiliano Zapata",
                   cve_edo = "17",
                   geocoded_datasets = z,
                   zoom = 13,
                   status_caso = c(1, 2),
                   week = seq(from = lubridate::epiweek(Sys.Date())-2, to = lubridate::epiweek(Sys.Date()), by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")
```
:::


:::

</body>
</html>


## [Transmisión Activa del Dengue en Chiapas]{ style="color:#003300;"}
<hr style="height:2px;border-width:0;color:#330019;background-color:#330019">

<html>
<head>
```{=html}
<style>
.grid-container {
  display: grid;
  grid-template-columns: auto auto auto auto;
  padding: 5px;
}

.grid-item {
  background-color: rgba(255, 255, 255, 0.8);
  border: 1px solid rgba(0, 0, 0, 0.8);
  padding: 5px;
  font-size: 30px;
  text-align: center;
}
</style>
```
</head>
<body>
<h1>
</h1>
<p>
</p>

::: grid-container

::: grid-item
Semana
:::

::: grid-item
Tuxtla Gutierrez
:::

::: grid-item
Tapachula
:::

::: grid-item
San Cristobal de las Casas
:::

::: grid-item
Acumulado
:::

::: grid-item
```{r tuxtla_acumulado}
#| echo: false
#| output: true
densnv::mp_heatmap(locality = "Tuxtla Gutierrez",
                   cve_edo = "07",
                   geocoded_datasets = z,
                   zoom = 13,
                   status_caso = c(1, 2),
                   week = seq(from = 1, to = lubridate::epiweek(Sys.Date()), by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")
```

:::

::: grid-item
```{r tapachula_acumulado}
#| echo: false
#| output: true
# Step 1. load the dengue dataset geocoded ####
densnv::mp_heatmap(locality = "Tapachula de Cordoba y Ordoñez",
                   cve_edo = "07",
                   geocoded_datasets = z,
                   zoom = 13,
                   status_caso = c(1, 2),
                   week = seq(from = 1, to = lubridate::epiweek(Sys.Date()), by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")

```

:::

::: grid-item

```{r san_cristobal_acumulado}
#| echo: false
#| output: true
densnv::mp_heatmap(locality = "San Cristobal de las Casas",
                   cve_edo = "07",
                   geocoded_datasets = z,
                   zoom = 14,
                   status_caso = c(1, 2),
                   week = seq(from = 1, to = lubridate::epiweek(Sys.Date()), by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")
```
:::

::: grid-item
Último Més
:::

::: grid-item
```{r tuxtla_ultimo_mes}
#| echo: false
#| output: true
densnv::mp_heatmap(locality = "Tuxtla Gutierrez",
                   cve_edo = "07",
                   geocoded_datasets = z,
                   zoom = 13,
                   status_caso = c(1, 2),
                   week = seq(from = lubridate::epiweek(Sys.Date())-4, to = lubridate::epiweek(Sys.Date()), by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")
```

:::

::: grid-item
```{r tapachula_ultimo_mes}
#| echo: false
#| output: true
# Step 1. load the dengue dataset geocoded ####
densnv::mp_heatmap(locality = "Tapachula de Cordoba y Ordoñez",
                   cve_edo = "07",
                   geocoded_datasets = z,
                   zoom = 13,
                   status_caso = c(1, 2),
                   week = seq(from = lubridate::epiweek(Sys.Date())-4, to = lubridate::epiweek(Sys.Date()), by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")

```

:::

::: grid-item

```{r san_cristobal_ultimo_mes}
#| echo: false
#| output: true
densnv::mp_heatmap(locality = "San Cristobal de las Casas",
                   cve_edo = "07",
                   geocoded_datasets = z,
                   zoom = 14,
                   status_caso = c(1, 2),
                   week = seq(from = lubridate::epiweek(Sys.Date())-4, to = lubridate::epiweek(Sys.Date()), by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")
```
:::

::: grid-item
Últimas dos Semanas
:::

::: grid-item
```{r tuxtla_ultimas_dos_semanas}
#| echo: false
#| output: true
densnv::mp_heatmap(locality = "Tuxtla Gutierrez",
                   cve_edo = "07",
                   geocoded_datasets = z,
                   zoom = 13,
                   status_caso = c(1, 2),
                   week = seq(from = lubridate::epiweek(Sys.Date())-2, to = lubridate::epiweek(Sys.Date()), by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")

```

:::

::: grid-item
```{r tapachula_ultimas_dos_semanas}
#| echo: false
#| output: true
# Step 1. load the dengue dataset geocoded ####
densnv::mp_heatmap(locality = "Tapachula de Cordoba y Ordoñez",
                   cve_edo = "07",
                   geocoded_datasets = z,
                   zoom = 13,
                   status_caso = c(1, 2),
                   week = seq(from = lubridate::epiweek(Sys.Date())-2, to = lubridate::epiweek(Sys.Date()), by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")
```

:::

::: grid-item

```{r san_cristobal_ultimas_dos_semanas}
#| echo: false
#| output: true
densnv::mp_heatmap(locality = "San Cristobal de las Casas",
                   cve_edo = "07",
                   geocoded_datasets = z,
                   zoom = 14,
                   status_caso = c(1, 2),
                   week = seq(from = lubridate::epiweek(Sys.Date())-3, to = lubridate::epiweek(Sys.Date()), by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")
```
:::


:::

</body>
</html>


## [Transmisión Activa del Dengue en Chiapas]{ style="color:#003300;"}
<hr style="height:2px;border-width:0;color:#330019;background-color:#330019">

<html>
<head>
```{=html}
<style>
.grid-container {
  display: grid;
  grid-template-columns: auto auto auto auto;
  padding: 5px;
}

.grid-item {
  background-color: rgba(255, 255, 255, 0.8);
  border: 1px solid rgba(0, 0, 0, 0.8);
  padding: 5px;
  font-size: 30px;
  text-align: center;
}
</style>
```
</head>
<body>
<h1>
</h1>
<p>
</p>

::: grid-container

::: grid-item
Semana
:::

::: grid-item
Palenque
:::

::: grid-item
Comitan de Domínguez
:::

::: grid-item
Huixtla
:::

::: grid-item
Acumulado
:::

::: grid-item
```{r palenque_acumulado}
#| echo: false
#| output: true
densnv::mp_heatmap(locality = "Palenque",
                   cve_edo = "07",
                   geocoded_datasets = z,
                   zoom = 14,
                   status_caso = c(1, 2),
                   week = seq(from = 1, to = lubridate::epiweek(Sys.Date()), by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")
```

:::

::: grid-item
```{r comitan_acumulado}
#| echo: false
#| output: true
# Step 1. load the dengue dataset geocoded ####
densnv::mp_heatmap(locality = "Comitan de Dominguez",
                   cve_edo = "07",
                   geocoded_datasets = z,
                   zoom = 13,
                   status_caso = c(1, 2),
                   week = seq(from = 1, to = lubridate::epiweek(Sys.Date()), by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")


```

:::

::: grid-item

```{r huixtla_acumulado}
#| echo: false
#| output: true
densnv::mp_heatmap(locality = "Huixtla",
                   cve_edo = "07",
                   geocoded_datasets = z,
                   zoom = 14,
                   status_caso = c(1, 2),
                   week = seq(from = 1, to = lubridate::epiweek(Sys.Date()), by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")
```
:::

::: grid-item
Último Mes
:::

::: grid-item
```{r palenque_ultimo_mes}
#| echo: false
#| output: true
densnv::mp_heatmap(locality = "Palenque",
                   cve_edo = "07",
                   geocoded_datasets = z,
                   zoom = 14,
                   status_caso = c(1, 2),
                   week = seq(from = lubridate::epiweek(Sys.Date())-4, to = lubridate::epiweek(Sys.Date()), by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")
```

:::

::: grid-item
```{r comitan_ultimo_mes}
#| echo: false
#| output: true
# Step 1. load the dengue dataset geocoded ####
densnv::mp_heatmap(locality = "Comitan de Dominguez",
                   cve_edo = "07",
                   geocoded_datasets = z,
                   zoom = 13,
                   status_caso = c(1, 2),
                   week = seq(from = lubridate::epiweek(Sys.Date())-4, to = lubridate::epiweek(Sys.Date()), by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")


```

:::

::: grid-item

```{r huixtla_ultimo_mes}
#| echo: false
#| output: true
densnv::mp_heatmap(locality = "Huixtla",
                   cve_edo = "07",
                   geocoded_datasets = z,
                   zoom = 14,
                   status_caso = c(1, 2),
                   week = seq(from = lubridate::epiweek(Sys.Date())-4, to = lubridate::epiweek(Sys.Date()), by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")
```
:::

::: grid-item
Últimas Dos Semanas
:::

::: grid-item
```{r palenque_ultimas_dos_semanas}
#| echo: false
#| output: true
densnv::mp_heatmap(locality = "Palenque",
                   cve_edo = "07",
                   geocoded_datasets = z,
                   zoom = 14,
                   status_caso = c(1, 2),
                   week = seq(from = lubridate::epiweek(Sys.Date())-2, to = lubridate::epiweek(Sys.Date()), by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")

```

:::

::: grid-item
```{r comitan_ultimas_dos_semanas}
#| echo: false
#| output: true
# Step 1. load the dengue dataset geocoded ####
densnv::mp_heatmap(locality = "Comitan de Dominguez",
                   cve_edo = "07",
                   geocoded_datasets = z,
                   zoom = 13,
                   status_caso = c(1, 2),
                   week = seq(from = lubridate::epiweek(Sys.Date())-2, to = lubridate::epiweek(Sys.Date()), by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")

```

:::

::: grid-item

```{r huixtla_ultimas_dos_semanas}
#| echo: false
#| output: true
densnv::mp_heatmap(locality = "Huixtla",
                   cve_edo = "07",
                   geocoded_datasets = z,
                   zoom = 14,
                   status_caso = c(1, 2),
                   week = seq(from = lubridate::epiweek(Sys.Date())-2, 
                              to = lubridate::epiweek(Sys.Date()), 
                              by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")
```
:::


:::

</body>
</html>


## [Transmisión Activa del Dengue en Oaxaca]{ style="color:#003300;"}
<hr style="height:2px;border-width:0;color:#330019;background-color:#330019">

<html>
<head>
```{=html}
<style>
.grid-container_20 {
  display: grid;
  grid-template-columns: 200px auto auto auto auto;
  grid-auto-rows: auto;
  padding: 5px;
}

.grid-item_20 {
  background-color: rgba(255, 255, 255, 0.8);
  border: 1px solid rgba(0, 0, 0, 0.8);
  padding: 5px;
  font-size: 30px;
  text-align: center;
}
</style>
```
</head>
<body>
<h1>
</h1>
<p>
</p>

::: grid-container_20

::: grid-item_20
Semana
:::

::: grid-item_20
Valles Centrales
:::

::: grid-item_20
Puerto escondido
:::

::: grid-item_20
San Pedro Pochutla
:::

::: grid-item_20
Salina Cruz
:::


::: grid-item_20
Acumulado
:::

::: grid-item_20
```{r valles_centrales_acumulado}
#| echo: false
#| output: true
densnv::mp_heatmap(locality = c("Oaxaca de Juarez", 
                                "Santa María Atzompa",
                                "San Jacinto Amilpas",
                                "Santa Lucia del Camino",
                                "San Antonio de la Cal",
                                "Santa Cruz Xoxocotlán",
                                "San Agustin de las Juntas"),
                   cve_edo = "20",
                   geocoded_datasets = z,
                   zoom = 13,
                   status_caso = c(1, 2),
                   week = seq(from = 1, to = lubridate::epiweek(Sys.Date()), by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")
```
:::

::: grid-item_20
```{r puerto_escondido_acumulado}
#| echo: false
#| output: true
# Step 1. load the dengue dataset geocoded ####
densnv::mp_heatmap(locality = c("Puerto Escondido",
                                "Brisas de Zicatela"),
                   cve_edo = "20",
                   geocoded_datasets = z,
                   zoom = 13,
                   status_caso = c(1, 2),
                   week = seq(from = 1, to = lubridate::epiweek(Sys.Date()), by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")


```
:::

::: grid-item_20
```{r san_pedro_pochutla_acumulado}
#| echo: false
#| output: true
densnv::mp_heatmap(locality = "San Pedro Pochutla",
                   cve_edo = "20",
                   geocoded_datasets = z,
                   zoom = 15,
                   status_caso = c(1, 2),
                   week = seq(from = 1, to = lubridate::epiweek(Sys.Date()), by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")

```
:::

::: grid-item_20
```{r salina_cruz_acumulado}
#| echo: false
#| output: true
densnv::mp_heatmap(locality = c("Salina Cruz",
                                "salina del marquez"),
                   cve_edo = "20",
                   geocoded_datasets = z,
                   zoom = 13,
                   status_caso = c(1, 2),
                   week = seq(from = 1, to = lubridate::epiweek(Sys.Date()), by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")

```
:::



::: grid-item_20
Último Mes
:::

::: grid-item
```{r valles_centrales_ultimo_mes}
#| echo: false
#| output: true
densnv::mp_heatmap(locality = c("Oaxaca de Juarez", 
                                "Santa María Atzompa",
                                "San Jacinto Amilpas",
                                "Santa Lucia del Camino",
                                "San Antonio de la Cal",
                                "Santa Cruz Xoxocotlán",
                                "San Agustin de las Juntas"),
                   cve_edo = "20",
                   geocoded_datasets = z,
                   zoom = 13,
                   status_caso = c(1, 2),
                   week = seq(from = lubridate::epiweek(Sys.Date())-4, to = lubridate::epiweek(Sys.Date()), by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")
```
:::

::: grid-item_20
```{r puerto_escondido_ultimo_mes}
#| echo: false
#| output: true
# Step 1. load the dengue dataset geocoded ####
densnv::mp_heatmap(locality = c("Puerto Escondido",
                                "Brisas de Zicatela"),
                   cve_edo = "20",
                   geocoded_datasets = z,
                   zoom = 13,
                   status_caso = c(1, 2),
                   week = seq(from = lubridate::epiweek(Sys.Date())-4, to = lubridate::epiweek(Sys.Date()), by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")


```
:::

::: grid-item_20
```{r san_pedro_pochutla_ultimo_mes}
#| echo: false
#| output: true
densnv::mp_heatmap(locality = "San Pedro Pochutla",
                   cve_edo = "20",
                   geocoded_datasets = z,
                   zoom = 15,
                   status_caso = c( 2),
                   week = seq(from = lubridate::epiweek(Sys.Date())-4, to = lubridate::epiweek(Sys.Date()), by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")

```
:::

::: grid-item_20
```{r salina_cruz_ultimo_mes}
#| echo: false
#| output: true
densnv::mp_heatmap(locality = c("Salina Cruz",
                                "salina del marquez"),
                   cve_edo = "20",
                   geocoded_datasets = z,
                   zoom = 13,
                   status_caso = c(1, 2),
                   week = seq(from = lubridate::epiweek(Sys.Date())-4, to = lubridate::epiweek(Sys.Date()), by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")

```
:::


::: grid-item_20
Últimas dos Semanas
:::

::: grid-item_20
```{r valles_centrales_ultimas_dos_semanas}
#| echo: false
#| output: true
densnv::mp_heatmap(locality = c("Oaxaca de Juarez", 
                                "Santa María Atzompa",
                                "San Jacinto Amilpas",
                                "Santa Lucia del Camino",
                                "San Antonio de la Cal",
                                "Santa Cruz Xoxocotlán",
                                "San Agustin de las Juntas"),
                   cve_edo = "20",
                   geocoded_datasets = z,
                   zoom = 13,
                   status_caso = c(1, 2),
                   week = seq(from = lubridate::epiweek(Sys.Date())-3, to = lubridate::epiweek(Sys.Date()), by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")
```
:::

::: grid-item_20
```{r puerto_escondido_ultimas_dos_semanas}
#| echo: false
#| output: true
# Step 1. load the dengue dataset geocoded ####
densnv::mp_heatmap(locality = c("Puerto Escondido",
                                "Brisas de Zicatela"),
                   cve_edo = "20",
                   geocoded_datasets = z,
                   zoom = 13,
                   status_caso = c(1, 2),
                   week = seq(from = lubridate::epiweek(Sys.Date())-2, to = lubridate::epiweek(Sys.Date()), by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")


```
:::

::: grid-item_20
```{r san_pedro_pochutla_ultimas_dos_semanas}
#| echo: false
#| output: true
densnv::mp_heatmap(locality = "San Pedro Pochutla",
                   cve_edo = "20",
                   geocoded_datasets = z,
                   zoom = 15,
                   status_caso = c(2),
                   week = seq(from = lubridate::epiweek(Sys.Date())-2, to = lubridate::epiweek(Sys.Date()), by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")

```
:::

::: grid-item_20
```{r salina_cruz_ultimas_dos_semanas}
#| echo: false
#| output: true
densnv::mp_heatmap(locality = c("Salina Cruz",
                                "salina del marquez"),
                   cve_edo = "20",
                   geocoded_datasets = z,
                   zoom = 13,
                   status_caso = c(1, 2),
                   week = seq(from = lubridate::epiweek(Sys.Date())-2, to = lubridate::epiweek(Sys.Date()), by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")

```
:::


:::

</body>
</html>
